---
description: После правок в репозитории — коммит в git и команды деплоя на VPS
alwaysApply: true
---

# Коммит и деплой на VPS

Когда в этом проекте завершена порция осмысленных правок кода:

1. **Сразу** выполни `git status`, при необходимости `git add`, **`git commit`** с понятным сообщением (что и зачем), затем **`git push origin main`** (или актуальная ветка).
2. В ответ пользователю **всегда** добавь готовый блок **команд для VPS** (путь к проекту по умолчанию `~/crm`, при необходимости подставь другой):

```bash
cd ~/crm
git pull origin main
docker compose build
docker compose up -d
```

Если в `pull` попали **новые Prisma-миграции** (новые папки в `apps/backend/prisma/migrations/`):

```bash
# по желанию перед первой migrate или крупной миграцией:
docker compose exec db pg_dump -U biscrm biscrm > backup_$(date +%Y%m%d_%H%M).sql

docker compose exec backend npx prisma migrate deploy
```

**Не** предлагать на VPS: `prisma migrate reset`, `prisma db push`, `docker compose down -v`. Подробности — правило `database-safety.mdc`.

Кратко напомни: полная перезагрузка страницы (**Ctrl+F5**) после обновления фронта.
